<?xml version="1.0" encoding="utf-8"?>
<!--
    Shared Fluent styling for Computer Archaeologist.
    Everything here builds on the built-in WinUI 3 theme resources so Light, Dark, high-contrast and
    the user's accent colour all keep working without any custom palette.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Standard content card: rounded, layered, subtle border. -->
    <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- A card that invites interaction. -->
    <Style x:Key="InteractiveCardBorder" TargetType="Border" BasedOn="{StaticResource CardBorder}">
        <Setter Property="Padding" Value="20" />
    </Style>

    <!-- Muted surface used behind metrics and previews. -->
    <Style x:Key="SubtleCardBorder" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="12" />
    </Style>

    <!-- Compact pill used for the interestingness score. -->
    <Style x:Key="ScorePillBorder" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="SectionSpacing" TargetType="StackPanel">
        <Setter Property="Spacing" Value="12" />
    </Style>

    <Style x:Key="CardSpacing" TargetType="StackPanel">
        <Setter Property="Spacing" Value="8" />
    </Style>

    <Style x:Key="PageRoot" TargetType="Grid">
        <Setter Property="Padding" Value="28,20,28,28" />
    </Style>

    <!-- Monospaced block used for path and code previews. -->
    <Style x:Key="MonoTextBlock" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Cascadia Mono,Consolas,Courier New" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="IsTextSelectionEnabled" Value="True" />
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
    </Style>

    <!-- The timeline rail dot. -->
    <Style x:Key="TimelineDot" TargetType="Ellipse">
        <Setter Property="Width" Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="Fill" Value="{ThemeResource AccentFillColorDefaultBrush}" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Margin" Value="0,6,0,0" />
    </Style>

</ResourceDictionary>
